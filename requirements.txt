# Pins are exact on purpose — the demo must not break on a transitive release.

# Contract (S-2)
pydantic==2.13.4

# Interface (IF-1)
python-telegram-bot==22.8   # v20+ for the asyncio API
python-dotenv==1.2.3

# Dev / test
pytest==9.1.1
pytest-asyncio==1.4.0

# Backend foundation (BE-1)
strands-agents[ollama]==1.53.0
boto3==1.43.81
ollama==0.6.2
pillow==11.3.0

# The project itself, so the src/ layout imports without PYTHONPATH juggling.
-e .
